<section id="new-feature">
  <span class="icon-bullhorn"></span>
  You can now import YouTube playlists, head on over 
  to <a href="<%= "#{current_user.username}/playlists" %>">your main playlists</a> page to get started!
</section>

<section  id="playlists-main" class="landing">
  <h2>Your Playlists</h2>

  <% if current_user.playlists.count != 0 %>
    <ul>
      <% current_user.playlists.each do | playlist | %>
        <li>
          <a href="<%= "/#{current_user.username}/playlist/#{url_encode(playlist.playlist_name)}" %>"><%= playlist.playlist_name %></a>
        </li>
      <% end %>
    </ul>
  <% else %>
    Looks like you currently have 0 playlists, starting using tubalr and click the 
    playlist dropdown under the video player to get started!
  <% end %>
</section>